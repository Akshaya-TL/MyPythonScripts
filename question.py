# data structure of return?
 
  def func(num :int) -> [int] / (int) / {int}/[str] :
      return [1,2,3,4],'b'
  output ([1,2,3,4],'b')
  
  def func(num : int) -> list(int):
      return [1,2,3,4],'b'
  output type object not iterable ---type error



output depending on return [] 
